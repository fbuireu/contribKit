coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
    patch:
      default:
        informational: true

ignore:
  - "**/types.ts"
  - "**/*.d.ts"
  - "**/*.astro"
