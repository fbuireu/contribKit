coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
    patch:
      default:
        informational: true

ignore:
  - "**/*.d.ts"
  - "**/types.ts"
  - "**/*.astro"
