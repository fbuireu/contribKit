pre-commit:
  parallel: true
  commands:
    flutter-format:
      root: "app/"
      glob: "*.dart"
      run: dart format {staged_files}
      stage_fixed: true
    flutter-analyze:
      root: "app/"
      glob: "*.dart"
      run: flutter analyze --fatal-infos

pre-push:
  parallel: false
  commands:
    flutter-analyze:
      root: "app/"
      glob:
        - "*.dart"
        - "pubspec.yaml"
        - "analysis_options.yaml"
      run: flutter analyze --fatal-infos
    flutter-test:
      root: "app/"
      glob:
        - "*.dart"
        - "pubspec.yaml"
        - "analysis_options.yaml"
      run: flutter test
